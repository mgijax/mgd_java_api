#!/bin/sh

if [ "${MGICONFIG}" = "" ]
then
    MGICONFIG=/usr/local/mgi/live/mgiconfig
    export MGICONFIG
fi
. ${MGICONFIG}/master.config.sh

export LOG_DIR=/data/mgd_java_api/logs
export LOG_FILE=${LOG_DIR}/java_api.log
